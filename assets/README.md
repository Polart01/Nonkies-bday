# assets

This directory stores project assets.

Structure:
- music/ — audio files (songs, background music).
- photos/ — image files (photos, illustrations).

Notes:
- Git does not store empty directories, so there are placeholder files in each subdirectory (.gitkeep) to ensure they are committed.
- Add your audio and image files into the appropriate subfolders.
